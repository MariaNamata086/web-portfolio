export default function Loading() {
  return (
    <div className='grid min-h-[60vh] place-items-center px-[22px]'>
      <span className='font-[family-name:var(--font-mono)] text-[11px] tracking-[0.14em] text-[var(--ink-faint)] uppercase'>
        Loading
      </span>
    </div>
  );
}
